

To run these tests, you *MUST* have the datacube code checked out into a
sibling folder of DQTools.
i.e.
/
    workspace/
        datacube/
        DQTools/

Many of the settings files and imported modules are in the datacube codebase.